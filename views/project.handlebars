<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
 {{!-- <p>Created by {{user.name}} on {{format_date date_created}}</p> --}}
  <p>
    <span class="dollar-amount">This mural was installed in {{needed_funding}}!</span>
  </p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
</div>
